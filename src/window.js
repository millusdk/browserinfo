export function windowHeight() {
  return window.innerHeight;
}

export function windowScreenX() {
  return window.screenX;
}

export function windowWidth() {
  return window.innerWidth;
}

export default {
  windowHeight,
  windowScreenX,
  windowWidth,
}